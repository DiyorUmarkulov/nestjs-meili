{
  "name": "nestjs-meili",
  "version": "0.1.4",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "exports": {
    ".": {
      "import": "./src/index.js",
      "types": "./src/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf node_modules src/*.js src/*.d.ts",
    "install": "pnpm install",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "meilisearch": "^0.50.0",
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^9.0.0 || ^10.0.0",
    "@nestjs/core": "^9.0.0 || ^10.0.0"
  }
}
