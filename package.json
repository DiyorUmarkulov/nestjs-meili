{
  "name": "nestjs-meili",
  "version": "1.1.3",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "import": "./src/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf src/*.js src/*.d.ts src/**/*.js src/**/*.d.ts",
    "install": "pnpm install",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "meilisearch": "^0.50.0",
    "reflect-metadata": "^0.2.2",
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^9.0.0 || ^10.0.0",
    "@nestjs/core": "^9.0.0 || ^10.0.0"
  }
}
